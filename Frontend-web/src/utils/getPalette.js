import { useTheme } from "@mui/material";

const getPallette = useTheme();

/* 
Para utilizar los colores como exporta la paleta del tema, se usa asi
const paleta ? getPalette();
paleta.primary
*/

export default getPallette.palette;